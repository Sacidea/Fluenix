{
  "name": "fluenix",
  "private": true,
  "scripts": {
    "web": "pnpm --filter web dev",
    "mobile": "pnpm --filter mobile start",
    "backend": "pnpm --filter backend dev",
    "seed": "pnpm --filter backend run seed",
    "ai": "cd apps/ai-service && uvicorn main:app --reload"
  }
}